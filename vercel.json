{
  "buildCommand": "npm install && cd server && npm install && npm run prisma:generate && cd ../client && npm install && npm run build",
  "outputDirectory": "client/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}