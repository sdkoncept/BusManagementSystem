{
  "buildCommand": "npm install || exit 1; cd server && npm install || exit 1; npx prisma generate || exit 1; cd ../client && npm install || exit 1; npm run build || exit 1",
  "outputDirectory": "client/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}