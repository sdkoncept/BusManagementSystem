{
  "buildCommand": "npm install && cd server && npm install && npx prisma generate && cd ../client && npm install && npx vite build",
  "outputDirectory": "client/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}